import styled from '../../../theme';
import primaryTheme from '../../../theme/primaryTheme';

const WorkContainer = styled.div`
  position: relative;
  margin: 0 auto;
  text-align: center;
  z-index: 1;
`;

export {
  WorkContainer
};
